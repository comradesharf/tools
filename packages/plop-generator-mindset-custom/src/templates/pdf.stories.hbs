import { {{name}} } from '#app/_pdfs/{{name}}.tsx';
import { PDFViewer } from '@react-pdf/renderer';
import type { Meta, StoryObj } from '@storybook/react';

const meta = {
    component: {{name}},
    args: {},
    decorators: function Component(Story) {
        return (
            <PDFViewer style=\{{
                height: '100dvh',
                width: '100vw',
                border: 'none',
            }}>
                <Story/>
            </PDFViewer>
        );
    },
    parameters: {
        backgrounds: {
            default: 'dark',
        },
    },
} satisfies Meta<typeof {{name}}>;

export default meta;

type Story = StoryObj<typeof {{name}}>;

export const Primary: Story = {};
